{
  "name": "@trancendos/adapters",
  "version": "0.1.0",
  "private": true,
  "description": "Ports & Adapters — Hexagonal Architecture for Zero Vendor Lock-in",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}