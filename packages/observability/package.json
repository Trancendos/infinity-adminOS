{
  "name": "@trancendos/observability",
  "version": "0.1.0",
  "description": "Production-grade observability for the Trancendos platform — structured logging, Analytics Engine metrics, W3C trace propagation, health aggregation",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "lint": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}