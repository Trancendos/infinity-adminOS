{
  "name": "@infinity-os/nexus-gateway",
  "version": "0.1.0",
  "description": "Nexus Gateway - Cloudflare Worker API Gateway",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.60.0"
  }
}
