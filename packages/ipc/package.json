{
  "name": "@trancendos/ipc",
  "version": "0.1.0",
  "private": true,
  "description": "Typed inter-process communication between Cloudflare Workers via service bindings",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "lint": "eslint src/"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250312.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}