{
  "name": "@trancendos/worker-ws-api",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket API Worker — Real-time rooms via Durable Objects",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.81.1"
  }
}