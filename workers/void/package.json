{
  "name": "@infinity-os/worker-void",
  "version": "1.0.0",
  "description": "VOID — Quantum-Safe Secure Secret Store (Cloudflare Worker)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260307.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "^3.114.17"
  }
}