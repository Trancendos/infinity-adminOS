{
  "name": "@trancendos/worker-api-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "API Gateway Worker — CORS, rate limiting, caching, reverse proxy",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260329.1",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.10.0"
  }
}