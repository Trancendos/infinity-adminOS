{
  "name": "@trancendos/carbon-router",
  "version": "1.0.0",
  "description": "Carbon Router \u2014 carbon-aware traffic routing + sustainability analytics. DPID: DPID-ECO-CAR-001",
  "scripts": {
    "dev": "wrangler dev --port 3082",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "zod": "^3.22.0",
    "pino": "^8.16.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260405.1",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0",
    "vitest": "^4.0.18"
  }
}
