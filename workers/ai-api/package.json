{
  "name": "@trancendos/worker-ai-api",
  "version": "0.1.0",
  "private": true,
  "description": "AI API Worker — Chat completions and embeddings proxy",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.1",
    "wrangler": "^4.10.0"
  }
}