{
  "name": "@infinity-os/worker-infinity-one",
  "version": "1.0.0",
  "description": "INFINITY-ONE — Central Account Management Hub (Cloudflare Worker)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "^3.114.17"
  }
}