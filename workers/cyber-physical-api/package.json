{
  "name": "@trancendos/worker-cyber-physical-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cyber-Physical API \u2014 BER state, IoT devices, NIM pipeline backend [DPID-CYB-PHY-API-001]",
  "scripts": {
    "dev": "wrangler dev --port 3086",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.12.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0",
    "vitest": "^4.0.18"
  }
}
