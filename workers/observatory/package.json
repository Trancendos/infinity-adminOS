{
  "name": "@infinity-os/worker-observatory",
  "version": "1.0.0",
  "description": "OBSERVATORY — Knowledge Application & VAI Norman Hawkins (Advanced Analytics, Data Correlation, Doc Parsing/OCR, Metrics & Monitoring)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "^3.114.17"
  }
}
