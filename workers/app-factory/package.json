{
  "name": "@trancendos/worker-app-factory",
  "version": "0.1.0",
  "private": true,
  "description": "App Factory Worker — Tenant app provisioning and lifecycle management",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.10.0"
  }
}