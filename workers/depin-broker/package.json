{
  "name": "@trancendos/depin-broker",
  "version": "1.0.0",
  "description": "DePIN Broker — IoT device registry + DePIN network broker. DPID: DPID-ECO-DEP-001",
  "scripts": {
    "dev": "wrangler dev --port 3081",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "zod": "^3.22.0",
    "pino": "^8.16.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  }
}
