{
  "name": "@trancendos/ai-worker",
  "version": "0.1.0",
  "description": "Unified AI inference proxy — Workers AI, OpenAI, Anthropic with tenant isolation, rate limiting, caching",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.0.0"
  }
}