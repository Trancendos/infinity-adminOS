{
  "name": "@trancendos/worker-files-api",
  "version": "0.1.0",
  "private": true,
  "description": "Files API Worker — R2-backed file storage with auth",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^6.0.2",
    "vitest": "^4.0.18",
    "wrangler": "^4.81.1"
  }
}