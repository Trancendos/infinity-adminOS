{
  "name": "@infinity-ecosystem/security",
  "version": "0.1.0",
  "description": "Security services nanoservice — Isolated security services",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo 'Security service dev'",
    "build": "echo 'Build security service'",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "docker:build": "docker build -t infinity-ecosystem/security .",
    "docker:run": "docker run -p 3054:3054 infinity-ecosystem/security",
    "health": "curl -sf http://localhost:3054/health || exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "private": true,
  "segment": "security",
  "isolation": "strict",
  "apiContracts": [],
  "meshTopology": "isolated"
}
