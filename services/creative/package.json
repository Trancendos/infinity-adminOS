{
  "name": "@infinity-ecosystem/creative",
  "version": "0.1.0",
  "description": "Creative services nanoservice — Isolated creative and media services",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo 'Creative service dev'",
    "build": "echo 'Build creative service'",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "docker:build": "docker build -t infinity-ecosystem/creative .",
    "docker:run": "docker run -p 3051:3051 infinity-ecosystem/creative",
    "health": "curl -sf http://localhost:3051/health || exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "private": true,
  "segment": "creative",
  "isolation": "strict",
  "apiContracts": [],
  "meshTopology": "isolated"
}
