{
  "name": "@infinity-ecosystem/knowledge",
  "version": "0.1.0",
  "description": "Knowledge services nanoservice — Isolated knowledge and RAG services",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo 'Knowledge service dev'",
    "build": "echo 'Build knowledge service'",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "docker:build": "docker build -t infinity-ecosystem/knowledge .",
    "docker:run": "docker run -p 3053:3053 infinity-ecosystem/knowledge",
    "health": "curl -sf http://localhost:3053/health || exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "private": true,
  "segment": "knowledge",
  "isolation": "strict",
  "apiContracts": [],
  "meshTopology": "isolated"
}
