{
  "name": "@infinity-ecosystem/financial",
  "version": "0.1.0",
  "description": "Financial services nanoservice — Isolated financial services",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "echo 'Financial service dev'",
    "build": "echo 'Build financial service'",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "docker:build": "docker build -t infinity-ecosystem/financial .",
    "docker:run": "docker run -p 3052:3052 infinity-ecosystem/financial",
    "health": "curl -sf http://localhost:3052/health || exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "private": true,
  "segment": "financial",
  "isolation": "strict",
  "apiContracts": [],
  "meshTopology": "isolated"
}
